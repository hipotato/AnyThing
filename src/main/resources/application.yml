# Server 配置
server:
  port: 9090

# ---------------------------------- 配置环境 ---------------------------------
spring.profiles.active: prod

# ---------------------------------- logback -----------------------------------
logging:
  config: classpath:logback-boot.xml